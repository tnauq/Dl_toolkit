# fixtures/

Read-only inputs. Committed once from a desktop session; never regenerated,
reformatted, or synthesised. See AGENTS.md.

Wanted, in priority order:

1. A `*_dir.vpk` from any Source 2 game — unblocks the format smoke test today
2. Representative Deadlock `vdata` (hero, item, ability definitions)
3. A decompiled official Deadlock map — the input to `dl-fgd` and the source
   of the map bounding-box measurement
4. A `cvarlist` dump from the pinned build

Not committed here: CSDK binaries, full game depots. Staleness risk, not legal.
