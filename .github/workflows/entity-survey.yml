name: entity survey

# REPLACES the first version, which had two faults:
#
#   1. it converted with -oe keyvalues2, which stamps an id on EVERY element,
#      so the "elements carrying an id" figure was meaningless. This one uses
#      keyvalues2_noids, where an id appears ONLY on an element referenced
#      more than once. That count is the answer to "how much sharing is
#      there", which is what decides whether the emitter can grow paths.
#
#   2. its line scanner could not tell an array SCALAR item from an element
#      TYPE header, because both are a single quoted token on a line. The
#      difference is only visible on the NEXT line: an element is followed by
#      a brace. This version keeps a one-line lookahead, so the element type
#      census is trustworthy this time. Same fix for two-token lines, where
#      "name" "TypeName" (inline element) and "element" "someid" (a
#      reference inside an array) are otherwise identical in shape.
#
# THE QUESTION THIS RUN EXISTS TO ANSWER: how does a lane_marker_path hold
# its route? The keyvalues carry only lanenum and LaneSlot, so the ordered
# positions live somewhere else — as inline child elements, or as references
# to shared CMapPathNode elements. Inline means the emitter can grow into it
# with no id machinery. References mean ids come back, and MapEmitter's whole
# no-GUID simplification has to be reopened.
#
# It dumps VERBATIM subtrees for the entities that matter, so the answer is
# readable rather than inferred from counts.
#
# ADDED 2026-08-22, TEST 1 OF THE CVisibilityMgr QUESTION. The emitter leaves
# CVisibilityMgr's nodes array empty, which is why its path nodes stay inline
# and need no ids. Whether that array is REQUIRED is the open risk. First
# evidence: in dl_example, CMapWorld's children holds 767 references and
# CVisibilityMgr's nodes holds 765. If the array were a manifest the runtime
# depends on, those counts would match. This run diffs the two id sets and
# names what sits in one and not the other, so the discrepancy stops being a
# number and becomes a fact about which kinds of element the array skips.
#
# A LARGE, MEANINGFUL GAP is evidence the array is editor bookkeeping.
# A GAP OF EXACTLY NOTHING but a couple of odd singletons is evidence it is
# meant to be exhaustive, and the emitter probably has to populate it.

on: workflow_dispatch

env:
  FIXTURE: dl_example.vmap
  # Element types whose whole subtree gets captured verbatim.
  # CMapGroup and CMapInstance are NOT capture types, deliberately. The
  # dumper starts a capture at the outermost element of a capture type and
  # will not start another while inside one, so a group swallows everything
  # nested in it — and its own excerpt is then discarded, because a group has
  # no classname to match. That is exactly what hid 4 of the 8
  # citadel_zipline_path: a cap of 8 returned only 4, and dl_example has
  # exactly 2 CMapGroup. Family B is grouped; family A is not.
  #
  # DmeConnectionData is deliberately NOT here, 2026-08-23. It is nested
  # inside a CMapEntity, and a capture never starts while one is already
  # open, so adding it would produce zero excerpts and look like the fixture
  # has no connections. That is the same trap CMapGroup set. Connections are
  # collected by a dedicated pass instead, below, and the ENTITY excerpts
  # carry them inline as context.
  CAPTURE_TYPES: 'CMapEntity,CMapPath,CMapPathNode'
  # Classnames worth an excerpt, plus the path element types above.
  #
  # CHANGED 2026-08-22: swapped the path classnames, whose structure is now
  # settled, for the BRUSH ENTITIES. trigger_item_shop and trigger_team_base
  # carry model, local.origin and local.scales, so they are volumes rather
  # than points, and how a volume attaches to its geometry is the one
  # mechanism the emitter has not read. The previous run's nesting table is
  # the hypothesis: 150 of 222 CMapMesh sit under `children` rather than at
  # root, and `children -> CMapEntity` is 276, which is the shape of a brush
  # entity holding its mesh INLINE. If the dump confirms that, shops cost
  # nothing new — it is the same inline pattern already emitted for paths.
  # If instead `children` holds a guid, brush entities need ids and are a
  # different job.
  #
  # SECOND QUESTION IN THE SAME DUMP: is `model` authored or compiler-written?
  # func_nav_markup showed model = maps\dl_streets\entities\unnamed_14650_959.vmdl,
  # a generated name under the map's own directory, which looks baked on
  # export rather than typed by a level designer. Six trigger_item_shop exist
  # and only four carry a model. If the ones WITH a model differ from the ones
  # without in whether they hold a child mesh, that answers it.
  # CHANGED 2026-08-22 (third pass). The brush question is settled for shops;
  # this pass covers everything ELSE the map needs placing, to learn which are
  # POINTS and which are VOLUMES before writing tables for them. Anything
  # named trigger_* is probably a volume and needs a child mesh; the spawners
  # and ropes may be points.
  #
  # Known roles, from the user 2026-08-22:
  #   citadel_trigger_idol_return  urn dropoff AND spawn; the 2 alternate
  #   trigger_midboss_shield       the midboss pit
  #   citadel_item_powerup_spawner bridge buff; rolls one of 4; 2 on the map
  #   citadel_trigger_climb_rope   climbable rope
  #   trigger_catapult             jump pad, probably
  #   citadel_trigger_push         teleporter, probably
  #   func_regenerate              spawn-room regeneration
  #   trigger_tier3phase2_shield   patron phase 2
  #   citadel_trigger_speed_boost  zipline boost — believed GLOBAL, not placed
  #   citadel_zap_trigger          knocked off the zipline — same
  #
  # The last two are the interesting ones: if their subtrees show a real
  # volume with extents they are placed after all; if they are bare points
  # with empty children they are demo markers or global toggles.
  #
  # NOTE ON COUNTS: dl_example is a GYM, not a layout. It shows one of each
  # mechanism (1 powerup spawner, 1 rope, 1 catapult) where the real map has
  # more. Do not read its counts as a template for how many to place.
  # TWO ZIPLINE SYSTEMS. dl_example has 8 citadel_zipline_path for 4 lanes,
  # and the tuple table shows they are not a matched pair: two per lane with
  # DIFFERENT key schemas. One carries start_active, the other lacks the key
  # entirely; 146 of the 270 nodes carry one_way and always_usable, the other
  # 124 lack both. That is two distinct systems sharing a classname, not one
  # cable authored twice.
  #
  # Both subtrees are captured here so the difference can be read rather than
  # guessed. What to look for: node COUNT, the height the route runs at, the
  # keys each carries, and whether one of them is short and base-adjacent
  # (which would make it the reinforcement zip rather than the lane transit
  # line).
  #
  # MAX_EXCERPTS is 8, not 4: the capture takes the first N encountered, and
  # the first four turned out to be the SAME family — 4 paths, 31 child refs
  # each, lane_number 1/3/4/6, no targetname, no start_active, which accounts
  # for exactly the 124 nodes that lack one_way and always_usable. Family B,
  # the 146-node one with start_active, pathNodes, pathNodePinsEnabled and
  # real targetnames like 8785_Lane6, was never reached. 8 gets both.
  #
  # 2026-08-23: the zipline questions are all closed, so the excerpt budget
  # goes to the entities that OWN connections. A shop disables when its
  # guardian falls, so the output side lives on npc_barrack_boss and the
  # input side on trigger_item_shop, and seeing both ends is what says which
  # way round the wiring goes.
  #
  # 2026-08-23, round two. The first connection pass showed the wiring is NOT
  # guardian-to-shop: not one of the 89 connections is owned by an
  # npc_barrack_boss. Each shop owns a small RELAY NETWORK instead, so the
  # excerpt budget goes to the entities that make it up. logic_auto_citadel
  # is the one that starts shops 15 seconds into the game and is the only
  # citadel-specific member.
  #
  # 2026-08-23, round three. The IO questions are closed and wired. The last
  # unverified thing in the drop is the KEY SET of the objective classes:
  # batch13 writes npc_boss_tier3 with the keys read off npc_boss_tier2,
  # because tier3's own were never tabulated. A wrong or missing keyvalue is
  # ignored silently by the game, so it surfaces as an objective that does
  # nothing rather than as an error.
  CAPTURE_CLASSES: 'npc_boss_tier3,npc_boss_tier2,npc_barrack_boss,destroyable_building,info_super_trooper_spawn'
  # Property TUPLES to tabulate, as classname:key,key;classname:key
  #
  # THE QUESTION: is a lane_marker_path SHARED between the teams or one per
  # team? dl_example has 16 of them on a FOUR-lane map. Per-team would be 24
  # (4 lanes x 3 slots x 2 teams), shared-with-3-slots would be 12; neither
  # fits, so the assumption that there are three slots may itself be wrong.
  #
  # Strong hint already in hand: lane_marker_path carries NO teamnumber, while
  # every team-owned entity in the map does. That points at one shared path
  # walked in both directions. If each (lanenum, LaneSlot) pair appears ONCE,
  # it is shared and batch13 must NOT mirror lane paths — mirroring would lay
  # a second full-length route on top of the first. If each pair appears
  # TWICE, they are per-team and the mirroring is right.
  #
  # The slot count falls out of the same table: distinct LaneSlot values.
  # ZIPLINE NODE OWNERSHIP, the open question 2026-08-22. Nodes carry
  # teamnumber 2, 3 and 4 (38 of 270 at 4). Ownership is DYNAMIC in game —
  # a team that pushes can zip most of the map — so the authored value
  # cannot be the runtime state. Two readings:
  #
  #   a STARTING state, split near the middle and moved from there, or
  #   a fixed property, where 4 marks permanently neutral segments.
  #
  # `capturable` is the discriminator. If the 4s are capturable and the 2/3s
  # are not, ownership is a fixed property of the node. If everything is
  # capturable, it is a starting state. `corner_node`, `one_way` and
  # `always_usable` come along in the same table since they are free.
  # ONE SPEC PER CLASSNAME. The parser keys on classname, so a repeated one
  # silently overwrites the earlier — hence the single wide tuple below
  # rather than three narrow ones.
  # FAMILY A vs FAMILY B, the last zipline question. dl_example has 8
  # citadel_zipline_path for 4 lanes and they are the SAME FOUR CABLES in two
  # representations, not two systems:
  #
  #   A  31 CMapPathNode children, no targetname, no pathNodes
  #   B  no children at all; the route packed into a pathNodes STRING as
  #      rows of 9 floats (pos xyz, in-tangent xyz, out-tangent xyz) relative
  #      to the entity origin, plus targetname 8785_LaneN, start_active,
  #      pathNodePinsEnabled, pathNodeRadiusScales. Lives inside the 2
  #      CMapGroup, so probably pasted or imported as a unit.
  #
  # IF THEY OCCUPY THE SAME SPACE they are duplicates and one cable per lane
  # is right, which is what batch13 already emits. If they are in different
  # places they are two systems and batch13 emits half the map's ziplines.
  # ORIGIN is the discriminator: family B's route is relative to it, so two
  # representations of one cable would share it.
  #
  # 2026-08-23. THE QUESTION NOW IS ENTITY IO. dl_example has 89
  # DmeConnectionData and the emitter writes none, so a shop never learns its
  # guardian died. NEXT records the key NAMES — outputName, targetType,
  # targetName, inputName, delay, timesToFire — but NOT their TYPES, and a
  # keyvalues2 attribute carries its type inline. Guessing that targetType is
  # an int and delay a float is exactly the class of guess that has cost this
  # project four probe rounds today. So the block is read, not assumed.
  #
  # WHAT A GOOD RESULT LOOKS LIKE: one verbatim DmeConnectionData excerpt
  # showing every attribute with its type, plus the tuple table below saying
  # which outputs and inputs are actually used across all 89. The pair
  # (outputName, inputName) is the vocabulary; if it is a handful of repeated
  # values, the wiring is a pattern that can be generated rather than
  # authored per entity.
  # TUPLE_SPECS keys on CLASSNAME, which comes out of EditGameClassProps. A
  # DmeConnectionData has no classname, so it cannot be tabulated this way and
  # the connection pass below does it instead. Left pointed at the shop so the
  # table still says which shops carry a teamnumber.
  #
  # Every key batch13 currently writes on an objective, so a missing column
  # says "this class does not take that key" and a (missing) cell in an
  # otherwise-full column says the fixture leaves it empty. The 'Keys per
  # classname' section further down is the complement: it lists keys that
  # exist in the fixture and are NOT written here, which is the failure
  # direction nothing else would catch.
  TUPLE_SPECS: 'npc_boss_tier3:targetname,teamnumber,lanenum,BossName,CoverGroupID,subclass_name;npc_boss_tier2:targetname,teamnumber,lanenum,BossName,CoverGroupID,subclass_name;npc_barrack_boss:targetname,teamnumber,lanenum,BackdoorProtectionTrigger,LaneSide,subclass_name;destroyable_building:targetname,teamnumber,subclass_name'

  # Excerpts per kind, and lines per excerpt. A brush entity's subtree
  # contains an entire CDmePolygonMesh if the mesh is inline, which is
  # thousands of lines — the cap is deliberately BELOW that, because the
  # question is answered by the first few lines of `children`, not by the
  # vertex streams. A truncation notice is printed, so a truncated block is
  # itself evidence the mesh is inline.
  MAX_EXCERPTS: 4
  MAX_EXCERPT_LINES: 200

jobs:
  survey:
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Fixture is present and is a binary vmap
        shell: bash
        run: |
          set -uo pipefail
          mkdir -p work out
          [ -s "$FIXTURE" ] || { echo "FAIL: no $FIXTURE"; exit 1; }
          wc -c "$FIXTURE"
          echo -n "header: "; head -c 60 "$FIXTURE" | tr -d '\0'; echo

      - name: Fetch, reassemble, extract
        id: get
        shell: python
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          import os, re, json, hashlib, zipfile, sys, subprocess, shutil

          repo = os.environ['GITHUB_REPOSITORY']
          rel = json.loads(subprocess.run(
              ['gh', 'api', f'repos/{repo}/releases/tags/csdk-12'],
              capture_output=True, text=True, check=True).stdout)

          assets = rel['assets']
          if not assets:
              print("::error::no assets on release"); sys.exit(1)

          os.makedirs('dl', exist_ok=True)
          for a in assets:
              dest = os.path.join('dl', a['name'])
              with open(dest, 'wb') as fh:
                  subprocess.run(
                      ['gh', 'api', '-H', 'Accept: application/octet-stream',
                       f"repos/{repo}/releases/assets/{a['id']}"],
                      stdout=fh, check=True)

          files = [f for f in os.listdir('dl') if f.lower().endswith('.zip')]
          def ordinal(n):
              m = re.match(r'^bin\.cs2\.(\d+)\.zip$', n)
              return int(m.group(1)) if m else 1
          files.sort(key=ordinal)
          if not files:
              print("::error::no zip parts"); sys.exit(1)

          h = hashlib.sha256()
          with open('bin_cs2.zip', 'wb') as out:
              for f in files:
                  with open(os.path.join('dl', f), 'rb') as p:
                      for chunk in iter(lambda: p.read(1 << 20), b''):
                          out.write(chunk); h.update(chunk)
          sums = os.path.join('dl', 'SHA256SUMS.txt')
          if os.path.exists(sums):
              want = open(sums).read().split()[0]
              if want != h.hexdigest():
                  print("::error::checksum mismatch"); sys.exit(1)

          with zipfile.ZipFile('bin_cs2.zip') as z:
              z.extractall('csdk')
          ghosts = os.path.join('csdk', '__MACOSX')
          if os.path.isdir(ghosts):
              shutil.rmtree(ghosts)

          conv = ''
          for root, _, fs in os.walk('csdk'):
              for f in fs:
                  if f.lower() == 'dmxconvert.exe':
                      conv = os.path.abspath(os.path.join(root, f))
          if not conv:
              print("::error::dmxconvert.exe missing"); sys.exit(1)
          print("dmxconvert:", conv)
          with open(os.environ['GITHUB_OUTPUT'], 'a') as g:
              g.write(f"conv={conv}\n")

      # keyvalues2, NOT keyvalues2_noids, since 2026-08-29. noids strips the
      # element ids, and without ids an element cannot be linked back to
      # whatever references it - which is why three separate attempts to
      # attribute the 89 connections to their owners failed. The flag was
      # upstream of every one of those failures.
      #
      # The text is larger with ids in it (51 MB) and every consumer below
      # was written against the noids shape, so if a step here starts
      # reporting differently, this line is the first thing to look at.
      - name: Binary to keyvalues2 text
        timeout-minutes: 20
        shell: bash
        run: |
          set -uo pipefail
          ROOT="$(pwd -W 2>/dev/null || pwd)"
          RC=0
          "${{ steps.get.outputs.conv }}" \
            -i "$ROOT/$FIXTURE" -ie binary \
            -o "$ROOT/work/example.txt" -oe keyvalues2 > out/conv.txt 2>&1 || RC=$?
          cat out/conv.txt
          echo "exit: $RC" | tee -a out/conv.txt
          [ -s work/example.txt ] || { echo "FAIL: no text out"; exit 1; }
          wc -c work/example.txt | tee -a out/conv.txt

      - name: Survey
        timeout-minutes: 25
        shell: python
        run: |
          import re, os, json, collections

          SRC = 'work/example.txt'
          CAP_TYPES = set(os.environ['CAPTURE_TYPES'].split(','))
          CAP_CLASSES = set(os.environ['CAPTURE_CLASSES'].split(','))
          MAX_EX = int(os.environ['MAX_EXCERPTS'])
          MAX_LINES = int(os.environ['MAX_EXCERPT_LINES'])

          three = re.compile(r'^"([^"]*)"\s+"([^"]*)"\s+"(.*)"\s*,?$')
          two   = re.compile(r'^"([^"]*)"\s+"([^"]*)"\s*,?$')
          one   = re.compile(r'^"(.*)"\s*,?$')

          types    = collections.Counter()
          classes  = collections.Counter()
          keys     = collections.defaultdict(collections.Counter)
          ids      = 0
          refs     = collections.Counter()
          childof  = collections.Counter()

          # TEST 1 state. Every reference recorded against the element type
          # that holds it, so CMapWorld.children and CVisibilityMgr.nodes can
          # be compared as SETS rather than as counts. And every id-carrying
          # element described, so a gap can be named rather than counted.
          reflists = collections.defaultdict(list)   # "Owner.attr" -> [guid]
          idinfo   = {}                              # guid -> {type, classname}

          # classname -> {key,...} to tabulate, and the observed tuples.
          tuple_keys = {}
          for spec in os.environ.get('TUPLE_SPECS', '').split(';'):
              if not spec.strip():
                  continue
              cn, keys_csv = spec.split(':', 1)
              tuple_keys[cn] = keys_csv.split(',')
          tuples = collections.defaultdict(collections.Counter)

          # citadel_zipline_path rows: origin and child count, which the
          # tuple table cannot reach because both live OUTSIDE
          # entity_properties.
          ziprows = []
          zip_pending = None      # {'origin','kids','haspathnodes'} while inside one

          excerpts = collections.defaultdict(list)

          # Every DmeConnectionData found, as (owner frame, {attr: (type,
          # value)}). The owner frame is kept by reference, not copied, so the
          # classname it gains later is visible when the report is written.
          conns = []

          # Each open element gets a frame, so an id line and a classname
          # found later can both be attached to the right element.
          frames = []              # [{'type':..,'id':None,'class':None}]
          attr_stack = []
          props_depth = None
          cur_props = {}

          cap_lines = None
          cap_depth = None
          cap_type = None
          cap_class = None

          def flush_capture():
              global cap_lines, cap_depth, cap_type, cap_class
              if cap_lines is None:
                  return
              kind = cap_class or cap_type
              if (cap_type in CAP_TYPES and
                      (cap_class is None or cap_class in CAP_CLASSES) and
                      len(excerpts[kind]) < MAX_EX):
                  body = cap_lines[:MAX_LINES]
                  if len(cap_lines) > MAX_LINES:
                      body.append(f'... truncated, {len(cap_lines)} lines total')
                  excerpts[kind].append(body)
              cap_lines = cap_depth = cap_type = cap_class = None

          def owner_type():
              return frames[-1]['type'] if frames else '(root)'

          def lines_with_lookahead(fh):
              prev = None
              for raw in fh:
                  s = raw.rstrip('\r\n').strip()
                  if not s:
                      continue
                  if prev is not None:
                      yield prev, s
                  prev = s
              if prev is not None:
                  yield prev, ''

          depth = 0
          pending_type = None
          pending_attr = None

          with open(SRC, 'r', encoding='utf-8', errors='replace') as fh:
              for s, nxt in lines_with_lookahead(fh):
                  if cap_lines is not None and len(cap_lines) < MAX_LINES + 1:
                      cap_lines.append('\t' * max(0, depth - cap_depth) + s)

                  if s in ('{', '['):
                      depth += 1
                      if s == '{':
                          frames.append({'type': pending_type, 'id': None,
                                         'class': None})
                          if pending_type == 'CMapPath':
                              zip_pending = {'origin': '?', 'kids': 0,
                                             'lane': '?', 'name': '',
                                             'pathnodes': False,
                                             'depth': depth}
                          if pending_type == 'EditGameClassProps' and props_depth is None:
                              props_depth = depth
                              cur_props = {}
                      attr_stack.append(pending_attr)
                      pending_type = pending_attr = None
                      continue

                  if s in ('}', ']', '},', '],'):
                      closing_elem = s in ('}', '},')
                      if props_depth is not None and depth == props_depth:
                          cur_class = cur_props.pop('classname', '(none)')
                          classes[cur_class] += 1
                          for k, v in cur_props.items():
                              keys[cur_class][k] += 1
                          if cur_class in tuple_keys:
                              t = tuple(cur_props.get(k, '(missing)')
                                        for k in tuple_keys[cur_class])
                              tuples[cur_class][t] += 1
                          # EditGameClassProps is the frame being closed, so
                          # the classname belongs to the frame BELOW it.
                          if len(frames) >= 2:
                              frames[-2]['class'] = cur_class
                          if cap_lines is not None and cap_class is None:
                              cap_class = cur_class
                          props_depth = None
                      if (closing_elem and zip_pending is not None
                              and depth == zip_pending['depth']):
                          if zip_pending['lane'] != '?':
                              ziprows.append([
                                  zip_pending['name'] or '(none)',
                                  zip_pending['lane'],
                                  zip_pending['origin'],
                                  str(zip_pending['kids']),
                                  'B' if zip_pending['pathnodes'] else 'A'])
                          zip_pending = None
                      if closing_elem and frames:
                          f = frames.pop()
                          if f['type'] == 'DmeConnectionData' and f.get('conn'):
                              # The owner is the frame below, whose classname
                              # is filled in when ITS EditGameClassProps
                              # closes. That may not have happened yet, so the
                              # owning FRAME is stored and read at the end.
                              conns.append((frames[-1] if frames else None,
                                            f['conn']))
                          if f['id']:
                              idinfo[f['id']] = {'type': f['type'],
                                                 'classname': f['class'] or ''}
                      if attr_stack:
                          attr_stack.pop()
                      depth -= 1
                      if cap_lines is not None and depth < cap_depth:
                          flush_capture()
                      continue

                  m = three.match(s)
                  if m:
                      name, typ, val = m.groups()
                      # CONNECTION PASS. A DmeConnectionData carries its
                      # attributes directly, not inside an EditGameClassProps,
                      # so the classname machinery above cannot see it. Every
                      # attribute is recorded WITH ITS TYPE, because the type
                      # is the whole question: NEXT records the key names but
                      # not whether targetType is an int, delay a float, or
                      # either of them a string.
                      if frames and frames[-1]['type'] == 'DmeConnectionData':
                          frames[-1].setdefault('conn', {})[name] = (typ, val)
                      if zip_pending is not None:
                          if name == 'origin':
                              zip_pending['origin'] = val
                          elif name == 'lane_number':
                              zip_pending['lane'] = val
                          elif name == 'targetname':
                              zip_pending['name'] = val or '(none)'
                          elif name == 'pathNodes':
                              zip_pending['pathnodes'] = True
                      if name == 'id' and typ == 'elementid':
                          ids += 1
                          if frames:
                              frames[-1]['id'] = val
                      elif typ == 'element' and val:
                          refs[f'{name} (attribute)'] += 1
                          reflists[f'{owner_type()}.{name}'].append(val)
                      elif props_depth is not None and depth == props_depth:
                          cur_props[name] = val
                      continue

                  m = two.match(s)
                  if m:
                      a, b = m.groups()
                      if nxt in ('{', '['):
                          pending_attr = a
                          pending_type = b if nxt == '{' else None
                          if pending_type:
                              types[pending_type] += 1
                              childof[f'{a} -> {b}'] += 1
                              if (pending_type in CAP_TYPES and cap_lines is None):
                                  cap_lines = [s]
                                  cap_depth = depth + 1
                                  cap_type = pending_type
                                  cap_class = None
                      elif a == 'element':
                          holder = attr_stack[-1] if attr_stack else '(root)'
                          refs[f'{holder} (array item)'] += 1
                          if zip_pending is not None and holder == 'children':
                              zip_pending['kids'] += 1
                          # The array's own frame is the element that owns it.
                          reflists[f'{owner_type()}.{holder}'].append(b)
                      continue

                  m = one.match(s)
                  if m:
                      tok = m.group(1)
                      if nxt == '{':
                          types[tok] += 1
                          holder = attr_stack[-1] if attr_stack else '(root)'
                          childof[f'{holder} -> {tok}'] += 1
                          pending_type = tok
                          if tok in CAP_TYPES and cap_lines is None:
                              cap_lines = [s]
                              cap_depth = depth + 1
                              cap_type = tok
                              cap_class = None
                      continue

          flush_capture()

          def describe(guid):
              d = idinfo.get(guid)
              if not d:
                  return f'{guid} (never defined in this file)'
              cn = f" classname={d['classname']}" if d['classname'] else ''
              return f"{d['type']}{cn}"

          def w(fh, s=''):
              fh.write(s + '\n')

          with open('out/entity-survey.md', 'w', encoding='utf-8') as fh:
              w(fh, '# Entity survey of dl_example.vmap (keyvalues2, with ids)')
              w(fh)
              w(fh, f'elements referenced MORE THAN ONCE (carry an id): {ids}')
              w(fh)

              # ------------------------------------------------ TEST 1
              w(fh, '## Is CVisibilityMgr.nodes exhaustive?')
              w(fh)
              world = None
              vis = None
              for k in reflists:
                  if k.endswith('.children') and k.startswith('CMapWorld'):
                      world = k
                  if k.endswith('.nodes') and k.startswith('CVisibilityMgr'):
                      vis = k
              w(fh, '| array | references |')
              w(fh, '|---|---|')
              for k in sorted(reflists):
                  w(fh, f'| {k} | {len(reflists[k])} |')
              w(fh)
              if world and vis:
                  ws, vs = set(reflists[world]), set(reflists[vis])
                  only_w = ws - vs
                  only_v = vs - ws
                  w(fh, f'in world children but NOT in visibility nodes: {len(only_w)}')
                  for g in sorted(only_w):
                      w(fh, f'- {describe(g)}')
                  w(fh)
                  w(fh, f'in visibility nodes but NOT in world children: {len(only_v)}')
                  for g in sorted(only_v):
                      w(fh, f'- {describe(g)}')
                  w(fh)
                  # Path nodes are the case that matters: they are referenced
                  # from a CMapPath, not from the world. If the visibility
                  # array skips them, it is not a manifest of every element.
                  pathkids = set()
                  for k, v in reflists.items():
                      if k.startswith('CMapPath.'):
                          pathkids |= set(v)
                  w(fh, f'nodes referenced by a CMapPath: {len(pathkids)}')
                  w(fh, f'  of those, also in visibility nodes: {len(pathkids & vs)}')
                  w(fh, f'  of those, also in world children:  {len(pathkids & ws)}')
              else:
                  w(fh, 'Could not find both arrays. world=' + str(world) +
                        ' vis=' + str(vis))
              w(fh)

              # ------------------------------------------------ CONNECTIONS
              w(fh, '## Entity IO: every DmeConnectionData')
              w(fh)
              w(fh, f'found: {len(conns)}')
              w(fh)
              if not conns:
                  w(fh, 'NONE. The fixture is documented as having 89, so zero')
                  w(fh, 'here means the collector is broken, not that the map')
                  w(fh, 'has no wiring. Check that DmeConnectionData is NOT in')
                  w(fh, 'CAPTURE_TYPES: a capture swallows nested elements.')
              else:
                  # The attribute set with TYPES, which is what the emitter
                  # needs and what no note in the repo records.
                  shape = collections.Counter()
                  for _, c in conns:
                      shape[tuple(sorted((k, t) for k, (t, _) in c.items()))] += 1
                  w(fh, '### Attribute set and TYPES')
                  w(fh)
                  for sh, n in shape.most_common():
                      w(fh, f'{n} connection(s) with:')
                      for k, t in sh:
                          w(fh, f'  "{k}" "{t}"')
                      w(fh)
                  if len(shape) == 1:
                      w(fh, 'One shape across all of them, so the emitter can')
                      w(fh, 'write a fixed attribute set.')
                  else:
                      w(fh, f'{len(shape)} DIFFERENT shapes. The emitter cannot')
                      w(fh, 'assume a fixed set; optional attributes exist.')
                  w(fh)

                  # THE ENUM. Round one sampled ONE targetType, a 7 on a
                  # logic_relay, and an npc_boss_tier3 row looked different.
                  # An enum read from a single sample is a guess with a
                  # number in it, so every value is counted here against the
                  # owner and the target it names. If 7 always accompanies a
                  # plain targetname and some other value always accompanies
                  # something else, the meaning falls out of the pairing
                  # without needing a shipped table.
                  w(fh, '### targetType, every value')
                  w(fh)
                  tt = collections.Counter()
                  tt_owner = collections.defaultdict(collections.Counter)
                  for owner, c in conns:
                      v = c.get('targetType', ('', '(absent)'))[1]
                      tt[v] += 1
                      cls = (owner or {}).get('class') or (owner or {}).get('type') or '?'
                      tt_owner[v][cls] += 1
                  w(fh, '| targetType | count | owners |')
                  w(fh, '|---|---|---|')
                  for v, n in tt.most_common():
                      who = ', '.join(f'{k} x{m}'
                                      for k, m in tt_owner[v].most_common(6))
                      w(fh, f'| {v} | {n} | {who} |')
                  w(fh)
                  if len(tt) == 1:
                      w(fh, 'ONE value across all 89. The emitter can write it')
                      w(fh, 'as a constant and the enum does not matter.')
                  else:
                      w(fh, f'{len(tt)} distinct values, so it IS an enum and')
                      w(fh, 'the emitter cannot hardcode one. The owner column')
                      w(fh, 'is the evidence for what each value means.')
                  w(fh)

                  # Does every targetName resolve to an entity that exists?
                  # A connection naming something absent is either a fixture
                  # artefact or proof that names can point outside the map,
                  # and either way it is worth knowing BEFORE generating
                  # names by convention.
                  w(fh, '### Do connection targets exist in the map?')
                  w(fh)
                  targets = collections.Counter(
                      c.get('targetName', ('', ''))[1] for _, c in conns)
                  w(fh, f'{len(targets)} distinct target name(s).')
                  w(fh, 'Cross-check against the targetname index below is')
                  w(fh, 'manual for now: this pass records the names, it does')
                  w(fh, 'not resolve them.')
                  w(fh)
                  for t, n in targets.most_common(30):
                      w(fh, f'  {t or "(empty)"}  x{n}')
                  w(fh)

                  w(fh, '### Vocabulary: which outputs drive which inputs')
                  w(fh)
                  w(fh, '| owner classname | outputName | inputName | target'
                        ' | delay | times | count |')
                  w(fh, '|---|---|---|---|---|---|---|')
                  vocab = collections.Counter()
                  for owner, c in conns:
                      cls = (owner or {}).get('class') or (owner or {}).get('type') or '?'
                      row = (cls,
                             c.get('outputName', ('', ''))[1],
                             c.get('inputName', ('', ''))[1],
                             c.get('targetName', ('', ''))[1],
                             c.get('delay', ('', ''))[1],
                             c.get('timesToFire', ('', ''))[1])
                      vocab[row] += 1
                  for row, n in vocab.most_common(120):
                      cells = ' | '.join(x or '(empty)' for x in row)
                      w(fh, f'| {cells} | x{n} |')
                  w(fh)
                  w(fh, f'{len(vocab)} distinct row(s) across {len(conns)}'
                        ' connections. A small number means the wiring is a')
                  w(fh, 'PATTERN the emitter can generate rather than data')
                  w(fh, 'that has to be authored per entity.')
                  w(fh)

                  w(fh, '### One verbatim example')
                  w(fh)
                  w(fh, '```')
                  owner, c = conns[0]
                  cls = (owner or {}).get('class') or '?'
                  w(fh, f'owner: {cls}')
                  for k, (t, v) in c.items():
                      w(fh, f'"{k}" "{t}" "{v}"')
                  w(fh, '```')
              w(fh)

              w(fh, '## Property tuples')
              w(fh)
              for cn, keyset in tuple_keys.items():
                  w(fh, f'### {cn} — ' + ', '.join(keyset))
                  seen = tuples[cn]
                  if not seen:
                      w(fh, '- none found')
                      w(fh)
                      continue
                  w(fh, '')
                  w(fh, '| ' + ' | '.join(keyset) + ' | count |')
                  w(fh, '|' + '---|' * (len(keyset) + 1))
                  for t, c in sorted(seen.items()):
                      w(fh, '| ' + ' | '.join(t) + f' | {c} |')
                  total = sum(seen.values())
                  uniq = len(seen)
                  every_once = all(c == 1 for c in seen.values())
                  every_twice = all(c == 2 for c in seen.values())
                  w(fh, '')
                  w(fh, f'  {total} total, {uniq} distinct combination(s)')
                  if cn == 'lane_marker_path':
                      slots = sorted({t[keyset.index('LaneSlot')] for t in seen}) \
                          if 'LaneSlot' in keyset else []
                      lanes = sorted({t[keyset.index('lanenum')] for t in seen}) \
                          if 'lanenum' in keyset else []
                      w(fh, f'  lanes seen: {lanes}')
                      w(fh, f'  slots seen: {slots}')
                      if every_once:
                          w(fh, '  VERDICT: each (lane, slot) appears ONCE.')
                          w(fh, '  The path is SHARED between the teams and runs')
                          w(fh, '  the full length of the lane. batch13 must NOT')
                          w(fh, '  mirror lane paths.')
                      elif every_twice:
                          w(fh, '  VERDICT: each (lane, slot) appears TWICE.')
                          w(fh, '  Paths are PER TEAM and the mirroring is right.')
                      else:
                          w(fh, '  VERDICT: mixed counts — read the table, the')
                          w(fh, '  simple shared/per-team split does not hold.')
                  w(fh)

              w(fh, '## citadel_zipline_path origins')
              w(fh)
              w(fh, 'Two representations of ONE cable share an origin; two')
              w(fh, 'separate cables do not. Family B carries pathNodes and')
              w(fh, 'has no children.')
              w(fh)
              w(fh, '| targetname | lane | origin | children | family |')
              w(fh, '|---|---|---|---|---|')
              for row in ziprows:
                  w(fh, '| ' + ' | '.join(row) + ' |')
              w(fh)

              w(fh, '## Element types')
              w(fh, '')
              w(fh, '| count | type |')
              w(fh, '|---|---|')
              for t, c in types.most_common():
                  w(fh, f'| {c} | {t} |')
              w(fh)
              w(fh, '## Element references, by holder')
              w(fh)
              if refs:
                  for n, c in refs.most_common():
                      w(fh, f'- {n}: {c}')
              else:
                  w(fh, '- NONE. Nothing in this map is referenced by id.')
              w(fh)
              w(fh, '## Inline nesting: which types sit under which attribute')
              w(fh)
              for n, c in childof.most_common(60):
                  w(fh, f'- {n}: {c}')
              w(fh)
              w(fh, '## Classnames')
              w(fh)
              w(fh, '| count | classname |')
              w(fh, '|---|---|')
              for cn, c in classes.most_common():
                  w(fh, f'| {c} | {cn} |')
              w(fh)
              w(fh, '## Keys per classname')
              for cn, _ in classes.most_common():
                  w(fh)
                  w(fh, f'### {cn}')
                  for k, c in keys[cn].most_common():
                      w(fh, f'- {k} ({c})')

          with open('out/excerpts.md', 'w', encoding='utf-8') as fh:
              w(fh, '# Verbatim subtrees')
              w(fh)
              w(fh, 'Indented from the capture root. Truncated per the caps in env.')
              for kind, blocks in sorted(excerpts.items()):
                  for i, b in enumerate(blocks):
                      w(fh)
                      w(fh, f'## {kind} [{i}]')
                      w(fh)
                      w(fh, '```')
                      for ln in b:
                          w(fh, ln)
                      w(fh, '```')

          json.dump({
              'ids': ids,
              'types': dict(types),
              'refs': dict(refs),
              'reflists': {k: v for k, v in reflists.items()},
              'idinfo': idinfo,
              'childof': dict(childof),
              'classes': dict(classes),
              'tuples': {k: {'|'.join(t): c for t, c in v.items()}
                         for k, v in tuples.items()},
              'keys': {k: dict(v) for k, v in keys.items()},
          }, open('out/entity-survey.json', 'w'), indent=1)

          # A parse that is working reports 222 CMapMesh and 767 ids on this
          # fixture. Both were confirmed on the previous run. If either is
          # wrong, nothing else in the report is safe.
          print('CMapMesh:', types.get('CMapMesh'), '(want 222)')
          print('elements with an id:', ids, '(want 767)')
          print('described ids:', len(idinfo))
          for k in sorted(reflists):
              print(f'  {k}: {len(reflists[k])}')

      - name: What landed
        if: always()
        shell: bash
        run: |
          ls -l out || true

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: entity-survey
          path: out/
          if-no-files-found: ignore
