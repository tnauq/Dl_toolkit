<Project Sdk="Microsoft.NET.Sdk">

  <!-- REPLACES the version from the 2026-08-14 KV2 drop: adds the Contracts
       reference so `emit` can read a .mapplan.json. -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>Deadlock.MapSmoke</AssemblyName>
    <RootNamespace>Deadlock.MapSmoke</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Deadlock.Format\Deadlock.Format.csproj" />
    <ProjectReference Include="..\Deadlock.Contracts\Deadlock.Contracts.csproj" />
  </ItemGroup>

</Project>
